
void findAllSubstring(String strInput, String output, List<String> list) {
  
   if(str.length == 0) {
      list.add(output);
      return;
   }

  for(int i = 0; i<str.length -1; i++) {
     for (int j = i; j < str.length -1; j++
       findAllSubstring(strInput.substring(j), strInput(j));
     }
  }


}